/**
 * GridMaterial stub — we use StandardMaterial with DynamicTexture instead
 * of @babylonjs/materials GridMaterial to avoid an extra package dependency.
 * This file is kept as a re-export if we switch to the official GridMaterial later.
 */

// Intentionally empty — grid is rendered with procedural DynamicTexture in helpers.ts
export {};
